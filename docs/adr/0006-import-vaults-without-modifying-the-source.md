# Import vaults without modifying the source

Vault Import copies content into the dedicated Personal Data Directory before adding Personal OS identity and type metadata, leaving the source vault untouched by default. Import runs as a restartable background job with preflight counts, progress, diagnostics, and an end report; ordinary Markdown becomes Knowledge, while speculative Todo or Project classification is deferred to the Agent.
