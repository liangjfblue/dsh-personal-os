# Personal OS owns the sidebar

The client replaces the complete DSH `sidebar` slot so it can present its own identity and a coherent Conversation/My navigation model. This deliberately accepts responsibility for preserving the essential DSH session, workspace, collapsed-rail, and settings interactions instead of retaining the shipped sidebar shell.
