# Store one durable object per Markdown file

Each Entity and Capture has its own Markdown file, with a stable identity in Frontmatter and a human-readable filename that may change independently. Timeline uses daily Markdown files and attachments remain ordinary files; this favors direct editing, clear diffs, stable relations, and incremental indexing over compact multi-object documents.
